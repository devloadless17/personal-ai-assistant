{
  "name": "@assistant/e2e",
  "version": "0.1.0",
  "private": true,
  "description": "Playwright end-to-end suite — dashboard flows + full-stack webhook→agent→audit specs",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'e2e: nothing to lint yet'"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@types/node": "^24.3.1",
    "typescript": "^5.9.2"
  }
}
