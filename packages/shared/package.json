{
  "name": "@assistant/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript types/DTOs used by both the API and the dashboard",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "echo 'shared: nothing to lint yet'"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
